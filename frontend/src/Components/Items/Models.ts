export interface ItemCardRow {
    id: string;
    party: string;
    quantity: number;
    price: number
    address: string;
    date: string;
}

export interface ItemAreaProps {
    id: number;
}
